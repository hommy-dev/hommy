// The conversation is rendered by the persistent ThreadView in the messages
// layout (keyed off the URL), so this route renders nothing itself.
export default function ContractorThreadPage() {
  return null
}
